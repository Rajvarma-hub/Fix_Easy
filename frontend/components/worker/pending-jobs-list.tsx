"use client"

// This component is now superseded by available-jobs-page.tsx
// Re-export it as an alias for backwards compatibility with any page that imports it
export { AvailableJobsPage as PendingJobsList } from "./available-jobs-page"
